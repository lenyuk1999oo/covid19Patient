<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Choose patient</title>
</head>
<body>
<div style="text-align: center">
    <form method="post" action="/choose">
        <div>
            <label>Chose patient to change :
                <select name="patient">
                    {{#patients}}
                        <option value={{id}}>
                            {{name}}&nbsp;&nbsp;
                            {{surname}}&nbsp;&nbsp;
                            {{birthDate.year}}&nbsp;&nbsp;
                            {{birthDate.month}}&nbsp;&nbsp;
                            {{birthDate.day}}&nbsp;&nbsp;
                            {{patientStatus}}&nbsp;&nbsp;
                        </option>
                    {{/patients}}
                </select>
            </label>
            <button type="submit">Choose!</button>
        </div>
    </form>
</div>

<div><br><br><br></div>
<div style="text-align: center"><a href="/main">go to main page</a></div>

</body>
</html>